/*Pratique a sintaxe de multiplicação de números, uma vez utilizando o parâmetro this de um objeto criado por você, e depois sem ele.*/

const array = [1, 3, 5, 8, 12, 15];

array.map((item) => item * 2);

console.log(array)

